import RiscvRefinement.Publication.TeamB.MulhDiv.Common
import RiscvRefinement.Publication.TeamB.MulhDiv.HighMultiply
import RiscvRefinement.Publication.TeamB.MulhDiv.Division

/-!
# Team B multiplication/division publication façade

The common arithmetic, high-multiply, and division proof developments are
separate modules so each family can be kernel-checked independently.
-/
