//! Native CUDA trace construction for the simplified state-machine AIR.

pub const geometry = @import("geometry.zig");
pub const trace = @import("trace.zig");

test {
    _ = geometry;
    _ = trace;
}
