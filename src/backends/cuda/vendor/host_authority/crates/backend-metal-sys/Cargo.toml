[package]
name = "stwo-backend-metal-sys"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Metal GPU runtime and kernels for the STWO Metal proving backend"
links = "stwo_metal_runtime"

[dependencies]

[build-dependencies]
cc = "1.0"

[lib]
bench = false
