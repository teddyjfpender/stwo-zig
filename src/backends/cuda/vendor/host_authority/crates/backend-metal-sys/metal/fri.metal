// Native FRI fold kernels now live in the mirrored files:
// - `fold_circle_into_line.metal`
// - `fold_line.metal`
//
// This file stays present only to preserve the copied subtree history while the
// compile-active set moves to mirrored CUDA-compatible names.
