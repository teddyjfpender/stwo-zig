{
  "schema": "stwo-zig-package-contract-v2",
  "package": "stwo_prover_api",
  "version": "0.1.0",
  "layer": "api",
  "owner": "prover-api",
  "public_modules": {
    "stwo_prover_api": "mod.zig"
  },
  "dependencies": {
    "stwo_core": "../core"
  },
  "injected_modules": [],
  "api_surface": [
    "ColumnEvaluation",
    "ColumnSource",
    "CpuCompositionContentionPolicy",
    "CpuCompositionExecutionRequest",
    "DeviceCompositionStage",
    "ProveOptions",
    "QuotientOpsError",
    "TASK_PROFILE_SCHEMA_VERSION",
    "TaskProfile",
    "assertProverEngine",
    "column",
    "device_composition",
    "engine",
    "stage_profile",
    "task_profile"
  ],
  "api_contract": {
    "signature_tests": [
      "mod.zig::api signature: prover engine transaction is structurally checked"
    ],
    "invariant_tests": [
      "column.zig::column evaluation validates storage and lifting geometry",
      "stage_profile.zig::prover stage profile: task publication propagates capability errors",
      "task_profile.zig::task profile: abort releases exact reservation",
      "task_profile.zig::task profile: reservation and snapshot clean up every allocation failure",
      "task_profile.zig::task profile: reservation publishes by move and snapshot owns strings",
      "task_profile_reservation_test.zig::task profile reservation: generation exhaustion fails before allocation",
      "task_profile_reservation_test.zig::task profile reservation: malformed publication preserves capability",
      "task_profile_reservation_test.zig::task profile reservation: stale aborted copy cannot free replacement",
      "task_profile_reservation_test.zig::task profile reservation: stale published copy cannot affect next generation",
      "task_profile_reservation_test.zig::task profile reservation: wrong recorder publication is rejected"
    ]
  },
  "ci": {
    "lane": "prover_api",
    "host": "linux",
    "command": [
      "zig",
      "build",
      "test",
      "--build-file",
      "src/prover_api/build.zig",
      "-Doptimize=ReleaseFast",
      "-j2"
    ]
  }
}
