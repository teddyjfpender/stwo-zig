# Leaf device interactions: complete qualification

The actual detached leaf producer now supplies an explicit, admitted interaction
generator through shared core, noncore, transcript, statement, public, boundary,
and publication-input preparation. Host-default wrappers preserve existing callers.
No backend callback is retained in workspace state. Device profile availability
selects the route; device errors propagate without silently retrying on the CPU.
The authenticated inactive row 10 still uses its validated zero-column path.

The device generator checks the same framework shape and alias contracts as the
host writer and selects canonical leaf parameters from the admitted key. Exact
domain sums remain an independent host audit. Audited GPU output stages in the
existing inversion scratch before publication. A forced allocation failure after
four successful GPU dispatches leaves the destination unchanged; alias rejection
and successful retry are also covered. Input columns are projected from owned
logical rows, preserving their lifetime after trace commitment.

## Evidence

- 25 focused host tests and 14 recursion ownership checks passed.
- 74 leaf and 58 parent full/padded component cases passed exact column/claim
  parity, pole rejection and retry. Leaf domain sums match the host writer.
- Fresh CPU and Metal builds passed 384 complete-tree acceptance/rejection checks,
  including standalone verification after producer exit, malformed proofs, and
  same-geometry statement substitution under the original keys.
- All 21 serialized keys, claims and proofs are identical across CPU, Metal and
  the canonical baseline. Source snapshots match between complete gates.
- Each Metal leaf executes 36 active typed components / 144 dispatches; row 10
  remains inactive. Four leaves plus native tables and three parents account for
  1,020 required successful interaction dispatches.
- The freshly built AOT profile contains 105 additional exports / 271 total.
- This also completes the pending proof qualification of shared transaction
  storage and worker-pool ownership.

The JSON summaries contain binary hashes, admission pins, source snapshot,
per-case report hashes, complete build commands and local full-evidence paths.
`source.patch.gz` captures the cumulative authorized worktree used for both gates;
these report files and the subsequent documentation update are outside that
frozen build snapshot. The saved script records the qualification assertions.

## Complete-production observations

| Backend | Four leaves | Three parents | Combined production | Maximum RSS |
|---|---:|---:|---:|---:|
| CPU | 34.411 s | 32.171 s | 66.582 s | 3.898 GB |
| Metal | 27.807 s | 22.213 s | 50.019 s | 4.394 GB |

These are single observations, not controlled performance comparisons. Production
excludes compilation, shared-lock waits and hostile verification. The previous
shared-owner checkpoint observed Metal leaf production of 26.050 s; this run
establishes no leaf speedup. Cold program export and independent audit work remain
in the path. Optimize only after measuring those costs on complete requests.

This is the development q193 profile, not production-security admission. Legacy
outer-wrapper publication and tracked-allocator ownership still need a separate
cohesive review. The earlier 1/2/4/8 useful-memory ladder has not been rerun with
this newly integrated leaf device path.
