# Shared leaf owners qualification

The actual detached leaf preparation path now uses shared owners for the native FRI core, the noncore cohort, the complete 39-component cohort, and tuple diagnostics. Core preflight, witness/column preparation, interaction generation, cached receipts, and cohort publication live under `src/frontends/riscv/recursion`. Integration entry points retain their original public APIs as aliases; the core binding supplies its backend, and the cohort binding supplies the existing prepared leaf and core types.

This combined change moves 38 implementation files and removes 34 private CPU integration files. `transfer-audit.json` matches every moved file against its recorded transformation, ignoring whitespace after explicit module/type binding changes and promotion of tests to callable helpers. Dependency aliases resolve to the same source owners as before. All ten tests in the moved modules retain named integration wrappers, because Zig does not discover tests across named-module boundaries automatically. Original sources and transformation records are retained for review; the full source patch is the authoritative final change.

Validation: 29 focused ReleaseSafe test executions across the leaf, noncore, and cohort targets, plus 14 ownership checks. The source closure of the shared owners excludes concrete backend and integration files; standalone verifier dependency guards also pass. The initial ownership check caught a relocated authority import using an alias name instead of its actual `air/lang` path; it was corrected before qualification.

The complete CPU and Metal commands rebuilt artifacts from one frozen source snapshot, produced all seven nodes of the four-segment tree, exited producer processes, and verified serialized proofs in fresh processes. Each backend passed 192 acceptance/rejection checks including same-geometry statement substitution. All 21 serialized key/claim/proof artifacts remain identical across CPU/Metal and the canonical baseline. The required native-table and parent typed GPU dispatch checks remain active.

The protocol, challenge binding, exact domain audits, identity domains, prepared capture custody, two-owner staging, and cached publication rules are unchanged. There is no new speedup or production-security claim. The three related ownership moves were qualified together after short focused compilation loops, avoiding a full proof rebuild between each move.

Remaining work includes an ownership audit of transaction storage and the legacy outer-proof wrapper, and implementation of typed GPU interactions throughout the shared leaf path. The leaf still uses the existing interaction writer; moving its ownership does not activate GPU dispatch. Parent typed GPU dispatch and native-table GPU generation remain separately qualified.

`source.patch.gz` and `source-snapshot.json` freeze the qualified code before this report and the later goal-document update. CPU/Metal summaries pin artifact hashes, timings, and local full evidence directories. The focused and ownership logs, dependency audit, transfer audit, original-source archives, and transformation scripts record this checkpoint's scope and review trail.
