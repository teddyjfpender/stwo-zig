# Typed native boundary authority qualification

Independent typed definitions now admit program (ordinary/fixed), memory
(ordinary/full-state), clock, and all six lookup-table kinds at both native
capture and cold verifier VM profile reconstruction. Eleven policies cover nine
new infrastructure kinds. The fixed-program empty interaction batches and
full-state memory multiplicity distinction remain explicit. Table fixed tuples
remain authenticated by the verifier's preprocessed-root check and schema owner.

A shared symbolic comparison owner checks direct equations, interaction
recurrences, ordered event tuples and metadata for these providers, Merkle and
compact Poseidon. Protocol digests and pinned keys were not regenerated.

Focused tests passed: boundary 156 (one skipped), Merkle 150 (one skipped),
compact Poseidon 127, VM profile 21, source closure 37, inventory two. These
import-root counts overlap. The new boundary tests include 73 specialization
mutations, arbitrary extension-field rows and all-allocation-failure sweeps.

Fresh CPU/Metal/AOT four-segment products passed 384 acceptance/rejection checks;
all 21 artifacts match the canonical baseline. Both products used the identical
qualified source snapshot. See summary.json, backend summaries and retained logs.

Documentation was added after qualification. Wide Poseidon direct-equation
admission and final frontend/continuation qualification remain outstanding.
This development-parameter receipt does not establish production security.
