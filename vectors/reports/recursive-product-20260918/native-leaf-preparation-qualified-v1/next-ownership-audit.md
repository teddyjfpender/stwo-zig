# Remaining leaf ownership audit

Current qualified candidate moves the native leaf bundle to a generic shared owner. Remaining moves should follow the actual dependency graph:

1. Noncore owner: `recursive_segment_v2_noncore_owner.zig` (799 lines) uses private contract (252), support (637), and runtime (573). `rg` finds no external imports of those three private files; owner is their only entry. Contract has no leaf dependency. Support/runtime need only the prepared leaf type, authority trace owner, and leaf digest alias. Move the owner and its private implementation together; pass the shared leaf namespace explicitly. Preserve cold preparation, two-stage boundary trace storage, independent audit, retained generated receipt, and zero-allocation cached publication.
2. Core namespace: `recursive_fri_outer.zig` composes parts 00--27 and `recursive_fri_outer_manifest_generic_v4.zig`. Direct imports in the parts are generic prover APIs, std/core, plus two concrete bindings: part00 selects `stwo_cpu_backend` and the named frontend module; part18 imports `recursive_fri_component_parameters.zig`, which already only aliases shared `air.verifier_component_parameters`. Part00 constructs Engine from the selected backend. A shared core factory can take the backend explicitly, use narrow relative frontend imports, and consume the existing shared verifier parameters. Audit all exported aliases and test reachability before moving the namespace. Avoid using broad frontend aggregate imports to conceal new dependencies.
3. Leaf cohort/proof assembly: after the above owners move, remove remaining preparation algorithms from their integration facades and preserve standalone verifier dependency closure. The current proof still invokes `cohort.fillInteractionInto`; it has no typed GPU leaf generation route. Extend this only after shared ownership is qualified, preserving noncore/core staged generation before publication and the generated-receipt checks.

This is a source dependency audit, not evidence that the remaining moves are complete. Full CPU/Metal proof qualification is required after each cohesive ownership move.
