# Next recursive artifact: compatible wrapper3, then actual parent2/3

Prepared only. Source-v11 is the exact frozen source of the live wrapper2
candidate; its consuming proof has not been accepted yet. This request refreshes
the old geometry-v2 template to that source without changing code or inputs.
All5779 frozen files and all6 pair3/4/materialization pins were rehashed. Pair3/4
selects global3 at slot0. Its native3 proof and full metadata equal wrapper2's
context3; global2 end cycle equals global3 start cycle.

After wrapper2 guarded3/3 and its pinned external verifier pass, freeze the
successful checker receipt, trusted-run key pin and exact key file into a new
finalized launch request. No candidate-selected key/dimensions may supply the
comparison authority. Run the existing `test-ethereum-wrapper-geometry` guarded
command with one worker under the shared heavy lock. Both named tests are
explicit declarations in the genuine root. Current missing key env fields make
this request intentionally incomplete for launch.

The gate cold-opens retained native inputs, materializes global3, and constructs
its authenticated Geometry. It compares all10 fixed-wire dimensions against
those derived from the independently accepted wrapper2 key, then exits before
Complete cohort, tuple closure ledger, extra AIR row scans or PCS allocation.
It still incurs real Geometry plan/witness backing costs; it is not a zero-cost
shape query. No native or wrapper proof is produced.

A mismatch blocks wrapper3 production. Equality establishes the necessary wire
size/FRI geometry prerequisite; it does not establish equal verification keys,
AIR/session identities, successful wrapper3 proof, continuation acceptance or
parent proof. Next steps remain wrapper3 fresh acceptance then actual parent
whose AIR verifies both children. Wrapper2 independent verification has priority.
