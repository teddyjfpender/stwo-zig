# Bounded native verification scheduling

The explicit scheduling policy admits only the pinned fixed-program v5 native selected-leaf verifier, one worker, a 3 GiB monitored child envelope, 6 GiB host reserve, 180 second request deadline and 128 MiB input transport bound. The measured baseline includes native0 at 2,390,608,152 bytes peak physical footprint. These are monitored scheduling limits, not hard OS allocation limits. Host pressure and physical-footprint probes fail closed; resource failures drain only the verifier process group while retaining its lane lock.

Heavy builds, proofs, wrapper verification and final whole-bundle verification keep the existing global lock. No CSP defaults or worker policies changed. The controller still freshly verifies all accepted leaves on restart, reuses successful candidate production, and checks proof and metadata custody after verification. Scheduling identity is recorded per attempt, so the immutable campaign plan does not change.

Use the reviewed controller migration request, not the earlier standalone native19 request. Frozen Python source v3 passes all 37 bounded/controller tests; the active 45-test run additionally covers the unchanged global lock. Earlier incomplete closure snapshots remain retained but are not runnable release inputs.

Migration was authorized and executed after three childless samples of old controller 35106, verification of all 20 accepted proof hashes and candidate 19 custody, and confirmation that wrapper 40352 held the heavy lane. The new controller is 44583, supervised by 44582 under `.git/local-ethereum/bounded-native-controller-run-v1`. It must finish accepted-leaf rechecks before fresh candidate 19 verification. This migration is not proof or whole-block acceptance.

First real concurrent recheck passed: native leaf 0 completed in 87.195 seconds including scheduling/monitoring, with 2,390,526,184 bytes reported peak physical footprint. Wrapper 40352 remained live. This re-verifies an accepted leaf; unique accepted coverage remains 0–18 plus 120 until candidate 19 is freshly accepted. Exact first-recheck receipts and migration boundary checks are retained alongside this note.

The controller was then deliberately paused for the DevEx priority at 2026-09-08 06:26:01 UTC after 14 completed controller progress records (native 0–13); native 14 also completed its fresh verifier and execution receipt before the controller was signaled, giving 15 successful bounded verifier executions in total. A temporary bounded-lane guard waited for the active verifier to drain, held the next admission, and allowed a confirmed childless SIGTERM. Controller and supervisor are reaped, no native verifier or producer remains, all 20 accepted proof/metadata pairs and candidate 19 were rehashed unchanged. Resume with the recorded frozen migration argv after the focused DevEx gates; no receipt reuse was enabled.
