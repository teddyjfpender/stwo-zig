# Native119 reuse fallback (read-only assessment)

The running Metal campaign is unchanged. No separate119 proof or import is scheduled.

The frozen `metal-field5-block-v2/controller-source-v1/scripts/ethereum_full_leaf_bundle_producer.py:138` captures `retained_leaves` and `pending_attempts` for all121 indices at startup. Its loop does not discover later external publications. Hot-importing119 would not prevent its reproof and could cause a conflicting publication. Do not mutate live controller inventory to attempt this.

Use this fallback only if terminal wrapper becomes the next required artifact before native119 naturally completes:

1. At a confirmed zero-child lock-wait boundary following fresh acceptance, stop the controller process safely (currently PID35106), leaving its supervising launcher to reap it and write the terminal receipt. Verify controller termination and absence of proof/verifier children before importing. Do not stop only the supervisor, SIGSTOP a flock waiter, or interrupt an active proof. Retain the terminal receipt, immutable plan, logs, and any successful pending producer.
2. Produce119 in a separate output with the existing pinned Metalv7, authenticated121-segment geometry, profile `fixed_program_narrow_v5`, one worker, unchanged memory policy and AOT. Let the explicit producer create the absent selected119 admission at the existing selected-leaf admission root.
3. Destroy the producer and use the current pinned independent native verifier v6. Retain proof/metadata, source and executable identities, complete request/memory measurements, and fresh verification receipt.
4. After success, import identical proof bytes and canonical metadata into the stopped campaign using its existing immutable plan. Add a separate provenance/import receipt, preserving all prior receipts and accounting rather than rewriting them.
5. Resume the same pinned controller and plan with new launch/terminal logs. Its existing startup import path validates119 and freshly verifies it without reproving it. Other retained leaves are also freshly reverified, so this fallback has a material revalidation cost.

Current product pins: Metalv7 `9b16b0ad0f6cba79aa7582bb634055d43845bcb1f91781daaece8f7f1da53e1b`; native verifier v6 `7492946e72587e719cbd254de7b337bc381a63d62c4561eb61e6298c7e99c62c`. Recheck authoritative build receipts, source/plan/input pins, process state and inventory before any future use.
