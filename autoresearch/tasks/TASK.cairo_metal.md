<!-- GENERATED by `stwo-perf task --write` from autoresearch/MANIFEST.json and autoresearch/ledger/epochs.json. Do not edit by hand: edit the manifest, then regenerate. -->

# TASK — the `cairo_metal` track

Make the prover faster on this track's scored boundary, on this track's committed basket, without changing a single proof byte.

This brief is generated from `autoresearch/MANIFEST.json` and `autoresearch/ledger/epochs.json`. Those documents are the contract; this file is a view of them. Regenerate with `stwo-perf task --board cairo_metal`.

## Track state

- **Board**: `cairo_metal` · **group**: `cairo_metal` · **report schema**: `cairo_proof_v1`
- **Enabled**: no · **promotion eligible**: no
- **Build**: `zig build stwo-cairo-metal -Doptimize=ReleaseFast` → `zig-out/bin/stwo-cairo-metal`
- **Disabled because**: cairo_metal is staged only: TRACKS §2 records the Cairo + Metal product as parity_gated, not released. The product descriptor in build_support/products/cairo_metal.zig still carries state .parity_gated behind its test-cairo-metal-product and test-cairo-metal-oracle release gates, no Metal-lane Cairo calibration exists on the designated judge host, and the generalized per-board Metal calibration (TRACKS §7) is not yet built. The group stays dark and fails closed.
- **Promotion blocked because**: TRACKS §7: no cairo_metal calibration exists on the designated Apple M5 Max judge host, and the product is parity_gated rather than released.
- **Era**: none declared; the board falls back to global epoch `2`.
- **Scored boundary**: `prove_ms`

## Scored basket

| class | workload | unit |
| --- | --- | --- |
| `small` | `mcairo_all_opcodes` | committed cells |
| `wide` | `mcairo_all_builtins` | committed cells |

Scores are compared only inside one era on one board. A number from another track, another era, or another boundary is not a comparison (TRACKS §3, §7).

## Editable paths for THIS track

| path | minimum acceptance rung |
| --- | --- |
| `src/backend/proof_program.zig` | `s4` |
| `src/backends/cpu_scalar/**` | `s3` |
| `src/backends/cuda/**` | `s3` |
| `src/backends/metal/**` | `s3` |
| `src/core/crypto/**` | `s3` |
| `src/core/fields/**` | `s3` |
| `src/core/fri/**` | `s3` |
| `src/core/pcs/**` | `s3` |
| `src/frontends/cairo/**` | `s3` |
| `src/integrations/cairo_metal/**` | `s3` |
| `src/integrations/native_cuda/**` | `s3` |
| `src/prover/air/**` | `s3` |
| `src/prover/fft_pool.zig` | `s4` |
| `src/prover/fri.zig` | `s3` |
| `src/prover/pcs/**` | `s3` |
| `src/prover/poly/**` | `s3` |
| `src/prover/resident_storage.zig` | `s4` |
| `src/prover/session.zig` | `s4` |
| `src/prover/vcs_lifted/**` | `s3` |
| `src/prover/work_pool.zig` | `s4` |

Everything else is out of scope for this track. Frontend sources are editable **only** by their own track: a Cairo submission may edit `src/frontends/cairo/**`, and the same edit is an out-of-scope stray on a native or RISC-V submission (G2 fails).

Locked paths (never editable by any track): `autoresearch/**`, `scripts/**`, `vectors/**`, `conformance/**`, `build.zig`, `build.zig.zon`, `build/**`, `src/bench/**`, `src/tests/**`, `src/interop/**`, `.github/**`

## Regression guards

Registry `cairo` — 2 guard(s), run on this track's own binary (`zig-out/bin/stwo-cairo-metal`). A guard passes when its paired upper CI bound stays at or below `1.05`.

- `guard_cairo_all_builtins`
- `guard_cairo_all_opcodes`

Impact map (which edits select which guards):

| editable path prefix | guards |
| --- | --- |
| `src/frontends/cairo/` | all |
| `src/integrations/cairo_cpu/` | all |
| `src/integrations/cairo_metal/` | all |
| `src/integrations/cairo_cuda/` | all |
| `src/prover/` | all |
| `src/core/fri/` | all |
| `src/core/pcs/` | all |
| `src/core/fields/` | all |
| `src/core/crypto/` | all |
| `src/backends/cpu_scalar/` | all |
| `src/backends/metal/` | all |

An editable path that matches no rule selects **every** guard in the registry (fail closed).

## Correctness oracle

- **Authority**: `official-stwo-cairo-verifier`
- **Repository**: https://github.com/starkware-libs/stwo-cairo @ `82f21252a68ec006d73e299f5bf1ce6d4db0ee78`
- The oracle is the **final correctness authority**: a proof it refuses is not a proof, whatever the harness measured.

## Confirmation ladder

| tier | cost target | what it buys |
| --- | --- | --- |
| `T0` | 30 s | correctness smoke plus ONE stage-profiled sample on the proxy fixture; the claimed phase must move here before T1 is scheduled. One warmup is the floor, not a nicety: the Cairo arm records its mandatory phase profile on a discarded warmup, so warmups=0 would leave that track with no phase evidence at all. |
| `T1` | 300 s | paired ABBA on proxy fixtures with sequential early stopping and the PoW-excluded boundary; never ranks. |
| `T2` | 2700 s | full dual-boundary paired run on the contributor host, real classes, guards impact-mapped; the claimed verdict. |
| `T3` | judge-scheduled | judge-scheduled re-run plus audits on the designated host; the scored row. No cost target — the judge never trades honesty for time. |

T0 and T1 never rank; they gate scheduling. The claimed (T2) and ranked (T3) numbers always keep the full dual boundary — the ranked number never excludes anything (TRACKS §3.5).

## Participate

```sh
stwo-perf clone ../work && cd ../work
stwo-perf setup --board cairo_metal
stwo-perf ladder t0 --board cairo_metal --class small --phase <phase> --predecessor <tree>
stwo-perf ladder t1 --board cairo_metal --class small --predecessor <tree> --fast-boundary
stwo-perf run --board cairo_metal --class small --predecessor <tree>
stwo-perf submit
```

`--board cairo_metal` is explicit above because auto-routing only fires when the diff names a frontend or backend (TRACKS §8); a diff that spans two frontends is refused rather than routed.

**This track cannot promote today.** Runs still record evidence, and the ledger still serves history; no new row is scored until the manifest says otherwise.
