<!-- GENERATED by `stwo-perf task --write` from autoresearch/MANIFEST.json and autoresearch/ledger/epochs.json. Do not edit by hand: edit the manifest, then regenerate. -->

# TASK — the `core_metal` track

Make the prover faster on this track's scored boundary, on this track's committed basket, without changing a single proof byte.

This brief is generated from `autoresearch/MANIFEST.json` and `autoresearch/ledger/epochs.json`. Those documents are the contract; this file is a view of them. Regenerate with `stwo-perf task --board core_metal`.

## Track state

- **Board**: `core_metal` · **group**: `metal` · **report schema**: `native_proof_v7`
- **Enabled**: yes · **promotion eligible**: no
- **Build**: `zig build native-proof-bench-metal -Doptimize=ReleaseFast` → `zig-out/bin/native-proof-bench-metal`
- **Promotion blocked because**: TRACKS §6 retire-and-complete: the native Metal era is banked. New promotions refuse; the board stays enabled so its history, final scores, guard workloads, and PR6-supremacy cells keep running.
- **Retired** 2026-07-29T00:00:00Z: TRACKS §6 retire-and-complete: the native era is banked; history stays served; the native AIRs continue as PR6-supremacy cells and guard workloads
- **Era**: 2 (epoch `2`), status `banked`, opened 2026-07-21T16:00:00Z
- **Era opened because**: Metrics v2 opens audit-anchored scoring and the manifest-owned five-class native scoring universe; the Metal calibration freeze for this era is pinned in epoch 2's metal_calibration block
- **Scored boundary**: `prove_ms`
- **Banked**: this era closed 2026-07-29T00:00:00Z. Its scores are final and are never compared with a live track's (TRACKS §6).
- **Era note**: TRACKS §6 retire-and-complete: the native Metal era is banked. New promotions refuse, the full ledger and final scores stay served, and the closing audit stamps the last audited score on the M5 judge host.

## Scored basket

| class | workload | unit |
| --- | --- | --- |
| `small` | `mwf_log10x8` | trace rows |
| `wide` | `mwf_log14x32` | trace rows |
| `deep` | `mplonk_log14` | gates |
| `xlarge` | `mwf_log18x100` | trace rows |
| `huge` | `mwf_log20x100` | trace rows |

Scores are compared only inside one era on one board. A number from another track, another era, or another boundary is not a comparison (TRACKS §3, §7).

## Editable paths for THIS track

| path | minimum acceptance rung |
| --- | --- |
| `src/backend/proof_program.zig` | `s4` |
| `src/backends/cpu_scalar/**` | `s3` |
| `src/backends/cuda/**` | `s3` |
| `src/backends/metal/**` | `s3` |
| `src/core/crypto/**` | `s3` |
| `src/core/fields/**` | `s3` |
| `src/core/fri/**` | `s3` |
| `src/core/pcs/**` | `s3` |
| `src/integrations/native/**` | `s3` |
| `src/integrations/native_cuda/**` | `s3` |
| `src/prover/air/**` | `s3` |
| `src/prover/fft_pool.zig` | `s4` |
| `src/prover/fri.zig` | `s3` |
| `src/prover/pcs/**` | `s3` |
| `src/prover/poly/**` | `s3` |
| `src/prover/resident_storage.zig` | `s4` |
| `src/prover/session.zig` | `s4` |
| `src/prover/vcs_lifted/**` | `s3` |
| `src/prover/work_pool.zig` | `s4` |

Everything else is out of scope for this track. Frontend sources are editable **only** by their own track: a Cairo submission may edit `src/frontends/cairo/**`, and the same edit is an out-of-scope stray on a native or RISC-V submission (G2 fails).

Locked paths (never editable by any track): `autoresearch/**`, `scripts/**`, `vectors/**`, `conformance/**`, `build.zig`, `build.zig.zon`, `build/**`, `src/bench/**`, `src/tests/**`, `src/interop/**`, `.github/**`

## Regression guards

Registry `native` — 12 guard(s), run on this track's own binary (`zig-out/bin/native-proof-bench-metal`). A guard passes when its paired upper CI bound stays at or below `1.05`.

- `guard_blake_10x10`
- `guard_plonk_14`
- `guard_plonk_16`
- `guard_poseidon_10`
- `guard_poseidon_13`
- `guard_sm_v2_14`
- `guard_sm_v2_16`
- `guard_wf_10x8`
- `guard_wf_14x32`
- `guard_wf_16x64`
- `guard_xor_14`
- `guard_xor_16`

Impact map (which edits select which guards):

| editable path prefix | guards |
| --- | --- |
| `src/backends/metal/` | all |
| `src/prover/` | all |
| `src/core/fri/` | all |
| `src/core/pcs/` | all |
| `src/core/fields/` | all |
| `src/backends/cpu_scalar/` | all |
| `src/core/crypto/` | `guard_blake_10x10`, `guard_plonk_14`, `guard_poseidon_10`, `guard_poseidon_13`, `guard_wf_10x8`, `guard_wf_14x32` |

An editable path that matches no rule selects **every** guard in the registry (fail closed).

## Correctness oracle

This track declares no pinned correctness oracle in the manifest.

## Confirmation ladder

| tier | cost target | what it buys |
| --- | --- | --- |
| `T0` | 30 s | correctness smoke plus ONE stage-profiled sample on the proxy fixture; the claimed phase must move here before T1 is scheduled. One warmup is the floor, not a nicety: the Cairo arm records its mandatory phase profile on a discarded warmup, so warmups=0 would leave that track with no phase evidence at all. |
| `T1` | 300 s | paired ABBA on proxy fixtures with sequential early stopping and the PoW-excluded boundary; never ranks. |
| `T2` | 2700 s | full dual-boundary paired run on the contributor host, real classes, guards impact-mapped; the claimed verdict. |
| `T3` | judge-scheduled | judge-scheduled re-run plus audits on the designated host; the scored row. No cost target — the judge never trades honesty for time. |

T0 and T1 never rank; they gate scheduling. The claimed (T2) and ranked (T3) numbers always keep the full dual boundary — the ranked number never excludes anything (TRACKS §3.5).

## Participate

```sh
stwo-perf clone ../work && cd ../work
stwo-perf setup --board core_metal
stwo-perf ladder t0 --board core_metal --class small --phase <phase> --predecessor <tree>
stwo-perf ladder t1 --board core_metal --class small --predecessor <tree> --fast-boundary
stwo-perf run --board core_metal --class small --predecessor <tree>
stwo-perf submit
```

`--board core_metal` is explicit above because auto-routing only fires when the diff names a frontend or backend (TRACKS §8); a diff that spans two frontends is refused rather than routed.

**This track cannot promote today.** Runs still record evidence, and the ledger still serves history; no new row is scored until the manifest says otherwise.
