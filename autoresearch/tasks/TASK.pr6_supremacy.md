<!-- GENERATED by `stwo-perf task --write` from autoresearch/MANIFEST.json and autoresearch/ledger/epochs.json. Do not edit by hand: edit the manifest, then regenerate. -->

# TASK — the `pr6_supremacy` track

Make the prover faster on this track's scored boundary, on this track's committed basket, without changing a single proof byte.

This brief is generated from `autoresearch/MANIFEST.json` and `autoresearch/ledger/epochs.json`. Those documents are the contract; this file is a view of them. Regenerate with `stwo-perf task --board pr6_supremacy`.

## Track state

- **Board**: `pr6_supremacy` · **group**: `pr6_supremacy` · **report schema**: `pr6_supremacy_v1`
- **Enabled**: no · **promotion eligible**: no
- **Build**: `zig build benchmark-native-cpu native-proof-bench-metal -Doptimize=ReleaseFast` → `autoresearch/reference/measure_peer_series.py`
- **Disabled because**: PR6 Supremacy remains disabled until every exact workload port, log22 oracle vector, both timing boundaries, locked-M5 statistical gate, and authenticated judged verdict are complete.
- **Era**: none declared; the board falls back to global epoch `2`.
- **Scored boundary**: `prove_ms`

## Scored basket

| class | workload | unit |
| --- | --- | --- |
| `wide` | `pr6_wide_fibonacci_log14` | committed cells |
| `wide` | `pr6_wide_fibonacci_log16` | committed cells |
| `xlarge` | `pr6_wide_fibonacci_log18` | committed cells |
| `huge` | `pr6_wide_fibonacci_log20` | committed cells |
| `deep` | `pr6_blake_log10` | Blake scheduler rows |
| `deep` | `pr6_blake_log12` | Blake scheduler rows |
| `deep` | `pr6_blake_log14` | Blake scheduler rows |
| `deep` | `pr6_blake_log16` | Blake scheduler rows |
| `deep` | `pr6_plonk_log12` | Plonk gates |
| `deep` | `pr6_plonk_log14` | Plonk gates |
| `deep` | `pr6_plonk_log16` | Plonk gates |
| `small` | `pr6_fixed_wide_fibonacci_log4` | committed cells |
| `small` | `pr6_fixed_wide_fibonacci_log5` | committed cells |
| `small` | `pr6_fixed_wide_fibonacci_log6` | committed cells |
| `small` | `pr6_fixed_wide_fibonacci_log7` | committed cells |
| `small` | `pr6_fixed_wide_fibonacci_log8` | committed cells |
| `small` | `pr6_state_machine_log8` | state-machine rows |

Scores are compared only inside one era on one board. A number from another track, another era, or another boundary is not a comparison (TRACKS §3, §7).

## Editable paths for THIS track

| path | minimum acceptance rung |
| --- | --- |
| `src/backend/proof_program.zig` | `s4` |
| `src/backends/cpu_scalar/**` | `s3` |
| `src/backends/cuda/**` | `s3` |
| `src/backends/metal/**` | `s3` |
| `src/core/crypto/**` | `s3` |
| `src/core/fields/**` | `s3` |
| `src/core/fri/**` | `s3` |
| `src/core/pcs/**` | `s3` |
| `src/integrations/native/**` | `s3` |
| `src/integrations/native_cuda/**` | `s3` |
| `src/prover/air/**` | `s3` |
| `src/prover/fft_pool.zig` | `s4` |
| `src/prover/fri.zig` | `s3` |
| `src/prover/pcs/**` | `s3` |
| `src/prover/poly/**` | `s3` |
| `src/prover/resident_storage.zig` | `s4` |
| `src/prover/session.zig` | `s4` |
| `src/prover/vcs_lifted/**` | `s3` |
| `src/prover/work_pool.zig` | `s4` |

Everything else is out of scope for this track. Frontend sources are editable **only** by their own track: a Cairo submission may edit `src/frontends/cairo/**`, and the same edit is an out-of-scope stray on a native or RISC-V submission (G2 fails).

Locked paths (never editable by any track): `autoresearch/**`, `scripts/**`, `vectors/**`, `conformance/**`, `build.zig`, `build.zig.zon`, `build/**`, `src/bench/**`, `src/tests/**`, `src/interop/**`, `.github/**`

## Regression guards

This track binds **no guard registry**: TRACKS §3.4/§8: the supremacy board's driver is a peer-measurement script, not a bench binary that accepts guard args, and its 18 mandatory cells ARE the regression surface — every cell must win in both boundaries, so a separate guard portfolio would only re-measure the same statements.

## Correctness oracle

- **Authority**: `pinned-rust-stwo`
- **Repository**: https://github.com/starkware-libs/stwo @ `a8fcf4bdde3778ae72f1e6cfe61a38e2911648d2`
- **Performance peer**: https://github.com/ClementWalter/stwo @ `07ea1ccca13351028da94e66babf79e7ce91437f`
- **Peer toolchain**: `nightly-2025-07-14`
- The oracle is the **final correctness authority**: a proof it refuses is not a proof, whatever the harness measured.

## Confirmation ladder

| tier | cost target | what it buys |
| --- | --- | --- |
| `T0` | 30 s | correctness smoke plus ONE stage-profiled sample on the proxy fixture; the claimed phase must move here before T1 is scheduled. One warmup is the floor, not a nicety: the Cairo arm records its mandatory phase profile on a discarded warmup, so warmups=0 would leave that track with no phase evidence at all. |
| `T1` | 300 s | paired ABBA on proxy fixtures with sequential early stopping and the PoW-excluded boundary; never ranks. |
| `T2` | 2700 s | full dual-boundary paired run on the contributor host, real classes, guards impact-mapped; the claimed verdict. |
| `T3` | judge-scheduled | judge-scheduled re-run plus audits on the designated host; the scored row. No cost target — the judge never trades honesty for time. |

T0 and T1 never rank; they gate scheduling. The claimed (T2) and ranked (T3) numbers always keep the full dual boundary — the ranked number never excludes anything (TRACKS §3.5).

## Participate

```sh
stwo-perf clone ../work && cd ../work
stwo-perf setup --board pr6_supremacy
stwo-perf ladder t0 --board pr6_supremacy --class small --phase <phase> --predecessor <tree>
stwo-perf ladder t1 --board pr6_supremacy --class small --predecessor <tree> --fast-boundary
stwo-perf run --board pr6_supremacy --class small --predecessor <tree>
stwo-perf submit
```

`--board pr6_supremacy` is explicit above because auto-routing only fires when the diff names a frontend or backend (TRACKS §8); a diff that spans two frontends is refused rather than routed.

**This track cannot promote today.** Runs still record evidence, and the ledger still serves history; no new row is scored until the manifest says otherwise.
